git init
git initialize local git
git remote add origin
connects the remote repository to my local repository
git add .
add all files to staging

git status
check status for stages files
git commit
prepare files for push
git push --set-upstream origin main
upload local changes to remote github repository
create .env with apikey
create .gitignore with .env
